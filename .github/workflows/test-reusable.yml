name: Test Cross-Repo Reusable

on:
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  id-token: write

jobs:
  test:
    uses: astralform-ai/.github/.github/workflows/issue-triage.yml@main
    secrets: inherit
