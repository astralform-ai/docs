---
title: "Installation"
description: "Install the Astralform iOS SDK using Swift Package Manager or CocoaPods to add AI agent capabilities to your app."
---

## Requirements

- iOS 16.0+
- Xcode 15.0+
- Swift 5.9+

## Swift Package Manager

Add Astralform to your Xcode project:

1. Open your project in Xcode
2. Go to **File > Add Package Dependencies**
3. Enter the repository URL:
   ```
   https://github.com/astralform-ai/Astralform-iOS-SDK
   ```
4. Select **Up to Next Major Version** with `1.0.0`
5. Click **Add Package**

Or add it to your `Package.swift`:

```swift
dependencies: [
    .package(url: "https://github.com/astralform-ai/Astralform-iOS-SDK", from: "1.0.0")
]
```

Then add the dependency to your target:

```swift
.target(
    name: "YourApp",
    dependencies: ["Astralform"]
)
```

## CocoaPods

Add to your `Podfile`:

```ruby
pod 'Astralform', '~> 1.0'
```

Then run:

```bash
pod install
```

## Import the SDK

```swift
import Astralform
```

## Next Steps

<Card title="Configuration" icon="gear" href="/ios-sdk/configuration">
  Configure the client with your API key and settings
</Card>
